# CLAUDE.md

This project's instructions are tool-agnostic and live in `AGENTS.md`. Read it
first.

@AGENTS.md

## Claude-specific notes

- Comments: as few as possible — only what is truly necessary. Prefer none; at
  most a one-liner. If a comment genuinely needs more than one line, ask the user
  for permission first.
- Slash-command skills for the SDLC workflow live under `skills/` (design,
  implement, e2e) — scaffolding to be filled in.
- When making an architectural decision, add an ADR under `decisions/` using
  `decisions/template.md`.
