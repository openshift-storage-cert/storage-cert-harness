---
name: implement
description: Pick up a task, plan it, write tests and code (TDD), and open a PR for the storage-cert-harness. Use when implementing a scoped task.
---

# implement (scaffold)

> To be filled in. Intended phases: ingest → plan → code (TDD) → validate →
> publish → respond.

Until implemented, follow `docs/adapter-authoring.md` for tool work and
`docs/contributing.md` for the local checks and PR flow.
