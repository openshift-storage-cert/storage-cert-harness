version: "2"

run:
  modules-download-mode: vendor
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt

issues:
  exclude-dirs:
    - vendor
