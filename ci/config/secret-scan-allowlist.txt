# Exact added lines (git diff "+" body, no leading +) that secret-scan.sh should
# ignore when flagging SLA-like numerics.
#
# Prefer an inline token on the line (or the previous line) for Go/YAML:
#   secret-scan:ok  (canonical)
#   allow-numeric | ignore-gate | no-gate | nosecret  (aliases)
# Use this file only when a comment cannot be added (JSON).
# Keep empty unless a false positive is reviewed.
